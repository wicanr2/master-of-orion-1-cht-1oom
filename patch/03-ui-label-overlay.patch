diff --git a/src/ui/classic/uinewgame.c b/src/ui/classic/uinewgame.c
index 3100c0f..0e15a6b 100644
--- a/src/ui/classic/uinewgame.c
+++ b/src/ui/classic/uinewgame.c
@@ -100,6 +100,18 @@ static void new_game_draw_cb1(void *vptr)
     struct new_game_data_s *d = vptr;
     vgabuf_clear();
     lbxgfx_draw_frame(0, 0, d->gfx_newgame);
+    /* 繁中化:面板標籤烘在 gfx_newgame 背景圖裡。取樣面板色蓋掉英文後疊上 CJK。 */
+    {
+        static const struct { int y; const char *s; } lbl[3] = {
+            { 0x20, "銀河大小" }, { 0x47, "難度" }, { 0x6e, "對手" }
+        };
+        for (int i = 0; i < 3; ++i) {
+            uint8_t bg = *vgabuf_get_xy(0x60, lbl[i].y - 6); /* 取樣標籤上方面板色 */
+            vgabuf_draw_filled_rect(0x5e, lbl[i].y - 2, 0xa6, lbl[i].y + 9, bg);
+            lbxfont_select(0, 5, 0, 0);
+            lbxfont_print_str_normal(0x6c, lbl[i].y, lbl[i].s);
+        }
+    }
 }
 
 static void new_game_draw_race_cb(void *vptr)
@@ -260,8 +272,8 @@ bool ui_new_game(struct game_new_options_s *newopts)
         oi_gsize = uiobj_add_t0(0xaf, 0x1d, game_str_tbl_gsize[gsize], d.gfx_optb_ng, MOO_KEY_UNKNOWN, -1); \
         oi_diffic = uiobj_add_t0(0xaf, 0x44, game_str_tbl_diffic[diffic], d.gfx_optb_ng, MOO_KEY_UNKNOWN, -1); \
         oi_oppon = uiobj_add_t0(0xaf, 0x6b, game_str_tbl_oppon[oppon], d.gfx_optb_ng, MOO_KEY_UNKNOWN, -1); \
-        oi_cancel = uiobj_add_t0(0x5a, 0x93, "", d.gfx_optb_cancel, MOO_KEY_UNKNOWN, -1); \
-        oi_ok = uiobj_add_t0(0xa1, 0x93, "", d.gfx_optb_ok, MOO_KEY_UNKNOWN, -1); \
+        oi_cancel = uiobj_add_t0(0x5a, 0x93, "取消", d.gfx_optb_cancel, MOO_KEY_UNKNOWN, -1); \
+        oi_ok = uiobj_add_t0(0xa1, 0x93, "確定", d.gfx_optb_ok, MOO_KEY_UNKNOWN, -1); \
         oi_d = uiobj_add_inputkey(MOO_KEY_d); \
         oi_g = uiobj_add_inputkey(MOO_KEY_g); \
         oi_o = uiobj_add_inputkey(MOO_KEY_o); \
