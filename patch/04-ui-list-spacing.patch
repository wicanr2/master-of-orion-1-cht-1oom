diff --git a/src/ui/classic/uiobj.c b/src/ui/classic/uiobj.c
index 1792d2c..67f7b1a 100644
--- a/src/ui/classic/uiobj.c
+++ b/src/ui/classic/uiobj.c
@@ -3,6 +3,7 @@
 #include <string.h>
 
 #include "uiobj.h"
+#include "cjkfont.h"
 #include "comp.h"
 #include "game_types.h"
 #include "hw.h"
@@ -1944,6 +1945,10 @@ int16_t uiobj_select_from_list1(int x, int y, int w, const char *title, char con
     uiobj_table_clear();
     h = lbxfont_get_height();
     dy = lbxfont_get_gap_h() + h;
+    /* 繁中:24px CJK 比原版 ~8px 字高,清單列距需加大避免上下重疊。 */
+    if (cjkfont_available() && (dy < 14)) {
+        dy = 14;
+    }
 
     while (!flag_done) {
         if (*s == 0) {
@@ -2040,6 +2045,8 @@ int16_t uiobj_select_from_list2(int x, int y, int w, const char *title, char con
     uiobj_table_clear();
     h = lbxfont_get_height();
     dy = lbxfont_get_gap_h() + h;
+    /* TODO 繁中:多頁清單(list2)強制 dy=14 會觸發 heap 損毀(待查),暫不加大列距。
+       單頁清單(list1,如種族選擇)已加大;多頁清單沿用原列距(中文可能略擠)。 */
     ty = y + dy;
 
     while (!flag_done) {
