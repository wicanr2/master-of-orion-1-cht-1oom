diff --git a/src/ui/classic/uiobj.c b/src/ui/classic/uiobj.c
index 1792d2c..2756943 100644
--- a/src/ui/classic/uiobj.c
+++ b/src/ui/classic/uiobj.c
@@ -3,6 +3,7 @@
 #include <string.h>
 
 #include "uiobj.h"
+#include "cjkfont.h"
 #include "comp.h"
 #include "game_types.h"
 #include "hw.h"
@@ -1944,6 +1945,10 @@ int16_t uiobj_select_from_list1(int x, int y, int w, const char *title, char con
     uiobj_table_clear();
     h = lbxfont_get_height();
     dy = lbxfont_get_gap_h() + h;
+    /* 繁中:24px CJK 比原版 ~8px 字高,清單列距需加大避免上下重疊。 */
+    if (cjkfont_available() && (dy < 14)) {
+        dy = 14;
+    }
 
     while (!flag_done) {
         if (*s == 0) {
@@ -2040,6 +2045,11 @@ int16_t uiobj_select_from_list2(int x, int y, int w, const char *title, char con
     uiobj_table_clear();
     h = lbxfont_get_height();
     dy = lbxfont_get_gap_h() + h;
+    /* 繁中:多頁清單(如艦船設計武器/裝置選單)列距加大,避免 24px CJK 重疊。
+       (項數由 linenum 上限決定,dy 只改間距、不影響項數。) */
+    if (cjkfont_available() && (dy < 14)) {
+        dy = 14;
+    }
     ty = y + dy;
 
     while (!flag_done) {
